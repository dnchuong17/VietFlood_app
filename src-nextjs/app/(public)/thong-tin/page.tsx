import { ProfileView } from "@/features/auth/components/profile-view";

export const metadata = {
    title: "Thông tin tài khoản | VietFlood Insight",
};

export default function ProfilePage() {
    return <ProfileView />;
}
